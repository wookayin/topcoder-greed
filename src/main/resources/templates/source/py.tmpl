# -*- coding: utf-8 -*-

import array
import bisect
import collections
import fractions
import heapq
import itertools
import math
import re
import string

class ${ClassName}:
    def ${Method.Name}(self, ${Method.Params}):
        return ${Method.ReturnType;zeroval}

${CutBegin}
${<TestCode}
${CutEnd}
